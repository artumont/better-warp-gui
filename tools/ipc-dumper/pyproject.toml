[project]
name = "warp-ipc-dumper"
version = "0.1.0"
description = ""
authors = [
    {name = "artumont",email = "197291181+artumont@users.noreply.github.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[project.scripts]
ipc_dumper = "ipc_dumper.cli:main"
warp-ipc-dumper = "ipc_dumper.cli:main"

[tool.poetry]
packages = [{ include = "ipc_dumper" }]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
